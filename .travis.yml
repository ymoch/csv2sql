sudo: required
services:
  - docker
language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install coveralls
  - python setup.py install
script:
  - ./integrate.sh
after_success:
  - coveralls
notifications:
  - email: false
